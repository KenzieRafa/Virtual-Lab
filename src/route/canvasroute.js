import { generateRandomArray, startSorting, resetCanvas } from '../app/canvas.js';

window.generateRandomArray = generateRandomArray;
window.startSorting = startSorting;
window.resetCanvas = resetCanvas;